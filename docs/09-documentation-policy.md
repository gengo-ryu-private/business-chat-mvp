# ドキュメント管理方針

## 目的

README と `docs/` の役割を明確にし、情報の重複と README の肥大化を防ぐ。

## 基本方針

README は、初めてリポジトリを見る人がアプリ概要、目的、主な機能、技術的な見どころを短時間で把握するための入口とする。

`docs/` は、README では説明しきれない設計意図、要件、技術判断、テスト方針、確認手順を整理する場所とする。

## 判断基準

同じ説明を複数箇所に長く書かない。

新しい docs は、既存 docs に自然に収まらず、将来も参照する情報がある場合だけ追加する。
